
## Release Process Workflow
Whenever instructed to merge, commit, and push changes, automatically regenerate the upcoming release ZIP in the `dist` folder. Always clean out old versions from the `dist` folder to keep it tidy, so the user only has the latest ZIP ready to publish.
